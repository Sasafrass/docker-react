sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t Sasafras/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true Sasafras/docker-react npm run test